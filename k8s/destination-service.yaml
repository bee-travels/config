kind: Service
apiVersion: v1
metadata:
  name:  destination
  labels:
    run: destination
spec:
  selector:
    run:  destination
  type:  NodePort
  ports:
  - port: 80
    protocol: TCP
    targetPort:  4000